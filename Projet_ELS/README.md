# ELS

## Modules

- **Tesla Livraison** : l'ancien module livraison a ete retire au profit d'une variante basee sur l'interface Tesla (`Tesla_Livraison_*` + `Tesla.js`).  
  - Acces via `?page=livraison`, `?page=livreur` ou `?page=tesla-livraison`.  
  - Pas de sous-projet `livraison/` separe : la page est servie par le projet principal.
